# Chapter 22 skeptical predraft review dispositions

Date: 2026-08-18

Scope: the three skeptical reviews of prereview commit `b5edbe95fdcc0ed2469b70cdb77e80694129c579`. The prereview bundle remains immutable but is superseded. These dispositions govern the post-review package; they do not authorize manuscript prose by themselves.

## Claim completeness and schema

| Finding | Disposition | Resolution and gate |
|---|---|---|
| Regex extraction omitted qualitative trade-off rows and included setup/future questions. | Valid; resolved | Replaced the line-regex universe with three frozen, independent, read-only semantic manifests covering 21 + 17 + 8 reports. They contain 249 reviewed semantic claims, split mixed conclusions where the review identified them, and identify zero-claim reports explicitly (none). The generated JSON is only an exact index into those reviewed rows and inherited report contexts. |
| Dispositions did not use the canonical five-state vocabulary or agree with stale registers. | Valid; resolved | Every indexed claim has exactly one of `retained`, `qualified`, `superseded`, `rejected`, or `blocked`. Observation/design-option language remains non-authoritative manifest context. Mandatory contradiction and stale registers import the canonical status, and a generated reverse index is checked bidirectionally. |
| Decision cards checked only field truthiness. | Valid; resolved | Exact reviewed rows bind objective classification, alternatives, missing dimensions, safe replacement, limitation, current owner, and reversal/open condition. The validator checks the frozen manifest hash and exact row locator, permitted objective classes, mandatory bindings, and mandatory open outcome. |
| Local dominance was lexical rather than a comparability proof. | Valid; resolved conservatively | All 249 claims are dominance-ineligible. Chapter 22 has no portfolio-wide Pareto frontier and no local dominance result. A future local comparison would require a common producer, units, workload, alternatives, directions, constraints, and no decisive unknowns. |
| Report membership and metric-domain assignments could drift. | Valid; resolved | Exact report→claim lists, claim→report bindings, source hashes, manifest hashes/locators, per-report metric-domain sets, and claim-level domain text are validated. Count-preserving domain reassignment and membership mutations must fail. |

## Method and framing

| Finding | Disposition | Resolution and gate |
|---|---|---|
| Six families were filename buckets rather than recurring constraints. | Valid; resolved | Claims are classified into seven mechanism families independent of report chronology and portfolio domain. Every family spans at least two independent portfolio domains. Metrics and producers remain noncomposable across examples. |
| The mandatory stale/contradiction set was incomplete. | Valid; resolved | An exact eleven-class closure matrix covers dataflow, DRAM, double buffering, GBUF thresholds, inert SRAM arbitration, precision/rounding, fused activation, operator correctness/metrics, broadcast/topology, context scope, and scheduler semantics. Removal or re-affirmation is mutation-gated. |
| Alternatives were copied from family templates. | Valid; resolved | Alternatives now bind to each exact semantic claim row plus its reviewed report context. Pure observations remain open rather than receiving fabricated winners. The family register is only an index over those local bindings. |
| Metric domains were report identities and could hide composition. | Valid; resolved | The reviewed manifests assign claim-level/homogeneous-group domains such as local formula cycles, codec bytes/decoder estimates, conversion error, operator formulas, SRAM stall returns, functional structure, and traffic heuristic cycles. Eleven distinct noncomposable domains are indexed. |
| Unsupported physical, application-accuracy, and compiler/runtime recommendations remained affirmative. | Valid; resolved | The semantic manifests split or narrow these statements and assign `blocked`/`rejected` where no calibrated physical model, application metric, or executable bridge exists. Safe replacements preserve only bounded producer-local observations. |
| Sparse-distribution and NoC method claims lacked dedicated primary literature. | Valid; resolved by narrowing | Chapter 22 makes no literature-backed universal claim from these families. Sparse placement and topology reversal are retained only as pinned Tusim-local observations with exact current-book limitations. New broad methodological generalization would require a separate primary-source addition and review. |

## Evidence integrity and reproducibility

| Finding | Disposition | Resolution and gate |
|---|---|---|
| Runner could not reproduce the prereview bundle and had no post-review seal. | Valid; resolved in post-review workflow | The new audit path generates an exact payload manifest, checksum layer, immutable committed bundle, and outer seal binding the committed run tree. Verification is side-effect-free. |
| Book-side executable evidence was not pinned. | Valid; resolved | Execute mode requires clean `main`, records the exact book input commit, freezes all scripts, compiled book probes, reviewed manifests, inventory, framing/review records, and verifies their bytes against that commit. |
| Tusim preservation was prechecked only and not failure-safe. | Valid; resolved | Reconciliation captures exact HEAD, detached state, tracked/untracked status, and ignored inventory before and in `finally`; equality is gated. An injected early-failure control must retain matching before/after records. |
| Six-domain reconciliation checked sparse regex labels rather than complete contracts. | Valid; resolved | Operator invariants are checked independently in every repeat; scheduler validation parses all 15 topology/policy rows and all four columns; GBUF capacity retains eight exact reported rows, of which seven expose tested-grid arithmetic thresholds and K=64 is floor-censored by the 64 KiB sweep minimum; raw-log hashes are reopened by the validator. The immutable run's older “eight exact threshold rows” label is historical wording, not authority to erase the floor. |
| Repeat handling was not fail-closed. | Valid; resolved | Exactly three delimited operator repeats are required. Every repeat must satisfy correctness/pipeline invariants. Error magnitudes are retained without averaging; stability is an observation, not a required universal behavior. |
| Inventory hashes and exclusions were decorative. | Valid; resolved | The validator enumerates the complete pinned Markdown universe, verifies all 46 reports plus explicit exclusion, source hashes, and report line counts where declared. Corruption and count-preserving substitution mutations must fail. |
| Validator/mutations did not cover optimizer, assertion, path, exact membership, or semantic swaps. | Valid; resolved | The validator has no `assert` nodes, runs normally and with `python -O`, and executes 28 semantic mutations. The audit adds a real assertion-mutated validator control in both modes, exact member/hash checks, symlink/path rejection, failure-path preservation, and side-effect-free verification. |

## Closed framing after review

- Constraint-first organization remains binding.
- Dispositions are evidence filters, not the manuscript spine.
- No Chapter 21 sweep-construction tutorial is repeated.
- No report chronology, cross-domain scalarization, portfolio-wide Pareto frontier, uncalibrated physical metric, or compiler/runtime/ONNX composition is permitted.
- Prose authorization requires a green committed post-review bundle and passing outer-seal verification.
